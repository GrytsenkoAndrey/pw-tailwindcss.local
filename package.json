{
  "devDependencies": {
    "autoprefix": "^1.0.1",
    "postcss": "^8.4.18",
    "tailwindcss": "^3.2.1"
  },
  "scripts": {
    "build": "tailwind build -i style.css -o public/tailwind.css",
    "watch": "tailwind build -i style.css -o public/tailwind.css -w"
  }
}
